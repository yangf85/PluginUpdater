<Window
    x:Class="Cyclone.PluginUpdater.Views.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:vm="clr-namespace:Cyclone.PluginUpdater.ViewModels"
    Title="{Binding AppName, StringFormat={}{0} - 更新程序}"
    Width="480"
    Height="300"
    Background="#F0F0F0"
    FontFamily="Microsoft YaHei UI"
    FontSize="14"
    Icon="/app.png"
    ResizeMode="NoResize"
    WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibility" />
        <!--  主按钮（蓝色）  -->
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#0078D7" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderBrush" Value="#005A9E" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="16,6" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter
                                Margin="{TemplateBinding Padding}"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#006CBF" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#005FA8" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!--  次按钮（灰色）  -->
        <Style x:Key="SecondaryButton" TargetType="Button">
            <Setter Property="Background" Value="#E1E1E1" />
            <Setter Property="Foreground" Value="#1A1A1A" />
            <Setter Property="BorderBrush" Value="#ADADAD" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="16,6" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter
                                Margin="{TemplateBinding Padding}"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#D0D0D0" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#C0C0C0" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!--  进度条  -->
        <Style x:Key="FlatProgressBar" TargetType="ProgressBar">
            <Setter Property="Height" Value="4" />
            <Setter Property="Background" Value="#D8D8D8" />
            <Setter Property="Foreground" Value="#0078D7" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ProgressBar">
                        <Grid>
                            <Border Background="{TemplateBinding Background}" />
                            <Border x:Name="PART_Track" />
                            <Border
                                x:Name="PART_Indicator"
                                HorizontalAlignment="Left"
                                Background="{TemplateBinding Foreground}" />
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>

        <!--  顶部蓝色标题条  -->
        <Border
            Height="48"
            VerticalAlignment="Top"
            Background="#0078D7">
            <StackPanel
                Margin="20,0"
                VerticalAlignment="Center"
                Orientation="Horizontal">
                <TextBlock
                    VerticalAlignment="Center"
                    FontSize="15"
                    FontWeight="SemiBold"
                    Foreground="White"
                    Text="{Binding AppName}" />
                <TextBlock
                    VerticalAlignment="Center"
                    FontSize="15"
                    Foreground="#CCE4F7"
                    Text=" 更新程序" />
            </StackPanel>
        </Border>

        <!--  主内容区  -->
        <Grid Margin="24,64,24,24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!--  版本信息行  -->
            <Grid Grid.Row="0" Margin="0,0,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <TextBlock Foreground="#666666" Text="当前版本：" />
                    <TextBlock Foreground="#1A1A1A" Text="{Binding CurrentVersion}" />
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <TextBlock Foreground="#666666" Text="最新版本：" />
                    <TextBlock
                        FontWeight="SemiBold"
                        Foreground="#0078D7"
                        Text="{Binding LatestVersion}" />
                </StackPanel>
            </Grid>

            <!--  分割线  -->
            <Border
                Grid.Row="1"
                Height="1"
                Margin="0,0,0,12"
                Background="#D8D8D8" />

            <!--  状态文字  -->
            <TextBlock
                Grid.Row="2"
                Margin="0,0,0,12"
                Foreground="#333333"
                Text="{Binding StatusMessage}"
                TextWrapping="Wrap" />

            <!--  进度条（仅下载时显示）  -->
            <ProgressBar
                Grid.Row="3"
                Height="5"
                VerticalAlignment="Top"
                Maximum="100"
                Style="{StaticResource FlatProgressBar}"
                Visibility="{Binding IsDownloading, Converter={StaticResource BoolToVisibility}}"
                Value="{Binding DownloadProgress}" />

            <!--  底部按钮区  -->
            <StackPanel
                Grid.Row="4"
                HorizontalAlignment="Right"
                Orientation="Horizontal">
                <StackPanel.Resources>
                    <Style BasedOn="{StaticResource SecondaryButton}" TargetType="Button">
                        <Setter Property="Margin" Value="8,0,0,0" />
                    </Style>
                </StackPanel.Resources>

                <Button
                    Margin="0"
                    Background="Orange"
                    Command="{Binding ViewChangelogCommand}"
                    Content="查看更新内容"
                    IsEnabled="{Binding IsUpdateAvailable}"
                    Style="{StaticResource SecondaryButton}" />

                <Button
                    Margin="4,0"
                    Command="{Binding StartUpdateCommand}"
                    Content="立即更新"
                    IsEnabled="{Binding IsUpdateAvailable}"
                    Style="{StaticResource PrimaryButton}" />

                <Button
                    Click="CloseButton_Click"
                    Content="关闭"
                    Style="{StaticResource SecondaryButton}" />
            </StackPanel>
        </Grid>
    </Grid>
</Window>