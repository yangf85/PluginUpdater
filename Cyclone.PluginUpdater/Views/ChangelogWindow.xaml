<Window x:Class="Cyclone.PluginUpdater.Views.ChangelogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="更新内容"
        Width="560" Height="480"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Background="#F0F0F0"
        FontFamily="Microsoft YaHei UI">

    <Grid>

        <!-- 顶部蓝色标题条 -->
        <Border Height="40" VerticalAlignment="Top" Background="#0078D7">
            <TextBlock Text="更新内容"
                       Foreground="White" FontSize="14" FontWeight="SemiBold"
                       VerticalAlignment="Center" Margin="20,0" />
        </Border>

        <!-- WebBrowser 内容区 -->
        <Border Margin="16,56,16,56"
                BorderBrush="#D8D8D8"
                BorderThickness="1"
                Background="White">
            <WebBrowser x:Name="WebBrowserControl" />
        </Border>

        <!-- 底部关闭按钮 -->
        <Border Height="44" VerticalAlignment="Bottom"
                Background="#F0F0F0"
                BorderBrush="#D8D8D8"
                BorderThickness="0,1,0,0">
            <Button Content="关闭"
                    Width="88"
                    HorizontalAlignment="Right"
                    Margin="0,0,16,0"
                    Click="CloseButton_Click">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Background"      Value="#E1E1E1" />
                        <Setter Property="Foreground"      Value="#1A1A1A" />
                        <Setter Property="BorderBrush"     Value="#ADADAD" />
                        <Setter Property="BorderThickness" Value="1" />
                        <Setter Property="Padding"         Value="0,6" />
                        <Setter Property="Cursor"          Value="Hand" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}"
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            BorderThickness="{TemplateBinding BorderThickness}">
                                        <ContentPresenter HorizontalAlignment="Center"
                                                          VerticalAlignment="Center"
                                                          Margin="{TemplateBinding Padding}" />
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#D0D0D0" />
                                        </Trigger>
                                        <Trigger Property="IsPressed" Value="True">
                                            <Setter Property="Background" Value="#C0C0C0" />
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Button.Style>
            </Button>
        </Border>
    </Grid>
</Window>